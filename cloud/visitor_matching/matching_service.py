"""
Module: matching_service
Description: Matches visitors across cameras using embeddings.
"""

from sklearn.metrics.pairwise import cosine_similarity

def find_matching_embedding(new_embedding, existing_embeddings):
    """
    Finds the closest matching embedding.
    
    Args:
        new_embedding: The embedding to match.
        existing_embeddings: List of embeddings to compare against.
    """
    pass

def add_new_embedding(embedding):
    """
    Adds a new embedding to the database.
    
    Args:
        embedding: The embedding to add.
    """
    pass